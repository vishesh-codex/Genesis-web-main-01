# Database Configuration Template (Replace with your server credentials)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=genesis
DB_PORT=3306

# Authentication Security Secret
JWT_SECRET=genesis_jwt_secret_key_2025

# Environment Mode
NODE_ENV=development
